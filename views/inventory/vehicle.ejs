<section class="section new-cars" aria-labelledby="new-cars-label">
  <div class="container">
    <% if (title) { %>

      <% } else { res.redirect('/') } %>

        <%# messages() %>
          <%- carDetailsGrid %>
  </div>
</section>