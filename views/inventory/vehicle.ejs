<section class="section" aria-labelledby="new-cars-label">
  <div class="container">
    <% if (title) { %>

      <% } else { res.redirect('/') } %>

        <%# messages() %>
          <%- carDetailsGrid %>
  </div>
</section>