<% if (title) { %>
  <section class="section" aria-labelledby="new-make-label">
    <div class="container">
      <p class="section-subtitle :light" id="new-cars-label">Latest Cars</p>
      <h2 class="h2 section-title">
        <%= title %>
      </h2>
      <% } else { res.redirect('/') } %>

        <%# messages() %>
          <%- grid %>
    </div>
  </section>